import 'package:flutter/material.dart';

class AboutProducts extends StatelessWidget {
  const AboutProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Theme.of(context).colorScheme.background,
      ),
    );
  }
}
